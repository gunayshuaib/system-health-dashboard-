apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
name: system-health-dashboard
spec:
rules:
- host: dashboard.example.com
http:
paths:
- path: /
pathType: Prefix
backend:
service:
name: system-health-dashboard
port:
number: 5000